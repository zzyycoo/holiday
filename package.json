{
  "name": "booking-email-generator",
  "version": "3.1.1",
  "private": true,
  "type": "module",
  "description": "Hotel Service Booking Email Generator - Compact UI with Two-Column Layout",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^8.0.1"
  }
}
